import { Box, Container, Stack, Typography } from '@mui/material'

const Favorite = () => {
  return (
    <Container sx={{ paddingTop: { xs: '2rem', sm: '8rem' } }}>
      <Typography variant="h3" textAlign={'center'}>
        Favorite
      </Typography>
    </Container>
  )
}

export default Favorite
